version: "3.8"

services:
  photography-alexishayat-me-next:
    build: .
    container_name: photography-alexishayat-me-next
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    networks:
      - proxy

networks:
  proxy:
    external: true